from
  i := 0; j := n;
until i = j loop
  m := (i + j + 1) // 2 --there is infinite loop
  if t [m] <= x then
    i := m + 1
  else
    j := m
  end
end

if i >= 1 and i <= n then
  Result := (x = t [i])
else
  Result := False
end
